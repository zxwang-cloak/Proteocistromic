# Release checklist before journal submission

- [ ] Replace demo motifs/null data with manuscript-approved public demo materials if needed.
- [ ] Review `config/config.sh` and confirm all input directories.
- [ ] Validate `mergePeaks` availability in the release environment.
- [ ] Test `work.sh` from a clean clone.
- [ ] Record measured runtime if you want to replace placeholder values in the software checklist.
